-- Variante snowflake del schema (opcional).
-- Por ahora el proyecto usa schema_star.sql para NeonDB.
-- Aquí se puede definir un esquema normalizado con más dimensiones anidadas si se requiere.

-- Placeholder: mismo esquema estrella; en un snowflake típico habría más niveles (ej. dim_region, dim_category).
-- Ver schema_star.sql para el schema actual en uso.
